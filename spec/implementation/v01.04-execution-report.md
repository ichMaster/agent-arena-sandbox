# Phase v01.04 — Execution Report

**Date:** 2026-07-21 · **Branch:** Anthropic-Opus4.8-Sonet5-dev · **Source:** `spec/implementation/v01.04-issues.md`
**Target release:** v01.04.00 (tag `opus-sonnet-v01.04.00`) · **Executed by:** Claude Sonnet 5, via `/ship-solution`

File-driven execution — no GitHub involved. **This version is the v01 release gate.**

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed / Skipped / Remaining | 0 |

## Issues

| # | ARENA ID | Title | Status | Commit | Tests (suite) |
|---|----------|-------|--------|--------|---------------|
| 1 | ARENA-OPUS-SONNET-012 | ConnectionManager, WS envelopes & protocol | completed | b3314bd | 99 |
| 2 | ARENA-OPUS-SONNET-013 | WS endpoint — connect/token/joined/finally | completed | 900cd16 | 106 |
| 3 | ARENA-OPUS-SONNET-014 | `chat` action → `chat_message` broadcast | completed | a8632a0 | 108 |
| 4 | ARENA-OPUS-SONNET-015 | `submit_move` authority + `game_over` (gate) | completed | 5ba6685 | 115 |

Suite grew 81 → 99 → 106 → 108 → 115; `mypy --strict` clean (13 source files) throughout.

## Notable finding during execution (recorded in the reconciled issues file)

While implementing ARENA-OPUS-SONNET-013, a `TestClient` websocket-context exit was found to surface
as **task cancellation**, not a clean `WebSocketDisconnect` — an un-shielded `finally: await
release_seat(...)` was interrupted mid-commit, leaking the seat deterministically (5/5 reproductions).
Fixed with `asyncio.shield(...)`, matching architecture.md §10's own "regardless of how the loop
ended" requirement. See the `⟳ Reconciled` mark in `v01.04-issues.md` for the full reasoning on why
this belonged in this issue rather than being deferred to v05.01.

## Detailed Results

### ARENA-OPUS-SONNET-012 (`b3314bd`)
`server/websockets.py` — pure/DB-free `ConnectionManager` + the five event builders + `parse_action`.
Contract + unit tests (fake WebSocket doubles).

### ARENA-OPUS-SONNET-013 (`900cd16`)
`GET /ws/match/{id}?token=`: 4001 on bad/missing token, `joined` on connect, shielded `finally`
cleanup. Integration tests incl. the seat-reclaim-after-disconnect race found and fixed above.

### ARENA-OPUS-SONNET-014 (`a8632a0`)
Chat dispatch refactored into `_handle_action`; persists + broadcasts `chat_message`.

### ARENA-OPUS-SONNET-015 (`5ba6685`)
The full §5.4 authority flow + `Repository.finish_match` + `game_over`/`close_room`. Full-game (win,
draw), rejection-path, and terminal-`current_turn:null` integration tests, deterministic 5/5.

## DoD (roadmap §v01.04) — v01 release gate

> Two raw WS clients connect to one match, alternate server-validated legal moves, exchange chat, and
> reach `game_over` (room then closed); illegal, out-of-turn, and no-seat moves are rejected; the
> terminal `state_update` reports `current_turn: null`.

**Met.** No `architecture.md` edit — §5.3/§5.4/§6.2 implemented exactly as documented (the shield fix
is an implementation detail fulfilling §10's already-stated requirement, not a contract change).

## Next Steps
`review-and-fix-issues v01.04` → `release-version v01.04.00` (tag `opus-sonnet-v01.04.00`, no push) →
**END OF PHASE v01: HARDEN (automatic)** → v02.01 (the `LLMClient` seam).
