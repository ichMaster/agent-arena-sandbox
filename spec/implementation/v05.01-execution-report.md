# Phase v05.01 -- Execution Report

**Date:** 2026-08-16
**Branch:** codegen-tracking
**Label:** v05.01::phase
**Target release:** v05.01.00
**Executed by:** Claude Code (Sonnet 5)

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | ARENA ID | Title | Phase | Status | Commit | Files | Tests |
|---|----------|-------|-------|--------|--------|-------|-------|
| 1 | ARENA-107 | Per-socket write serialization for broadcast/send_to (deferred v01.04 finding) | v05.01 | completed | 8d488a4 | 3 | pass (223 full suite) |
| 2 | ARENA-108 | End-to-end reconnect: a new connection reclaims a released seat | v05.01 | completed | 753bac3 | 1 | pass (224 full suite) |
| 3 | ARENA-109 | Regression-guard the cancellation-safe cleanup primitive | v05.01 | completed | f58bc37 | 1 | pass (227 full suite) |

## Detailed Results

### ARENA-107: Per-socket write serialization
**Status:** completed · **Commit:** 8d488a4
**Validation:** [x] tests (223 full suite, 5 new concurrency tests confirmed to fail pre-fix) · [x] mypy --strict
Closes the deferred v01.04 finding. `ConnectionManager` now holds one `asyncio.Lock` per socket,
acquired around every `send_text`/`close` call. Updated `v01.04-code-review.md` in place per the
harden-findings convention, since this is that finding's actual landing.

### ARENA-108: End-to-end reconnect
**Status:** completed · **Commit:** 753bac3
**Validation:** [x] tests (224 full suite, 10x stress-tested) · [x] mypy --strict
Confirmation-only: the reconnect mechanism (`assign_symbol`/`release_seat`) already worked
correctly end to end through the real WS endpoint. No code fix needed -- a legitimate and valuable
outcome, now backed by a real integration test rather than an untested assumption.

### ARENA-109: Cancellation-safe cleanup regression guard
**Status:** completed · **Commit:** f58bc37
**Validation:** [x] tests (227 full suite, 10x stress-tested) · [x] mypy --strict
Three focused, deterministic tests directly targeting the primitive-choice regression class that
caused the original v01.04 bug (`asyncio.shield()` deadlocking where `anyio.CancelScope(shield=True)`
correctly protects the cleanup write) -- including a test proving `asyncio.shield()` specifically
does *not* work against an anyio-delivered cancellation, reproducing why the original fix needed
anyio's own primitive.

**Reconciliation note carried into this report:** four of the roadmap's v05.01 DoD lines
(dead-socket broadcast, malformed JSON, bad token, graceful-disconnect seat release) were already
satisfied by existing v01.03/v01.04 coverage before this phase started -- verified by reading the
real code and test suite first, per Step 0.5. This phase's three issues closed the genuinely open
gaps rather than re-doing already-solid work.

## Next Steps

v05.01 is fully implemented and green (227/227 tests, mypy --strict clean). Next:
`review-and-fix-issues v05.01`, then `release-version v05.01.00`.
