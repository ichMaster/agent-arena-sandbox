Version 05.03.00 (2026-08-16)
-----------------------------
- README.md (new): setup, all three run modes with exact commands, a WS
  protocol summary table, testing instructions, links to
  spec/game_specification.md and spec/architecture.md (ARENA-111)
- ARENA-112: genuinely simulated a clean checkout (fresh venv, pip install
  -e ".[dev]" from scratch) and ran every command README documents against
  it -- pytest, mypy, a real server start with /ui returning
  Cache-Control: no-store, and a scripts/run_arena.sh smoke test; zero
  drift found
- tests/test_ui_shell.py: automated the previously-manual checks that
  web/styles.css's design tokens match spec/ui_prototype.html exactly and
  that every DOM id web/app.js looks up exists in web/index.html
- tests/test_readme.py (new): 7 content-drift guards locking in README's
  structure
- v05.03 code review: clean pass, no findings
- Suite now 238 tests, all green, mypy --strict clean, zero paid API calls
- v05 release gate reached: the MVP is complete (roadmap.md)

Version 05.02.00 (2026-08-16)
-----------------------------
- tests/test_agent_vs_agent.py (new): two real AgentSessions, each
  ScriptedLLMClient-backed, play a full game to game_over over a real live
  server -- mirroring scripts/run_arena.sh's production shape but
  deterministic, free, and part of the default suite; plus an Observer
  watching that same match to completion without claiming a seat
- Reconciliation: confirmed the live-server fixture, human-vs-agent,
  observer join, disconnect/reconnect, and throwaway-DB + foreign_keys=ON
  coverage were already in place from v01.03-v05.01 -- agent-vs-agent was
  the one genuine gap this version closes
- v05.02 code review: clean pass, no findings
- Suite now 229 tests, all green, mypy --strict clean, zero paid API calls

Version 05.01.00 (2026-08-16)
-----------------------------
- server/websockets.py: ConnectionManager now serializes every send_text/
  close per socket with its own asyncio.Lock -- closes the finding deferred
  from v01.04 (broadcast() can be called by a different connection's task
  than the one that owns the recipient socket, with nothing previously
  serializing concurrent sends to it)
- End-to-end reconnect verified through the real WS endpoint: a disconnected
  participant reconnecting with the same token reclaims their seat
  (confirmation of existing behavior, backed by a new integration test)
- Regression-guarded the cancellation-safe cleanup primitive
  (anyio.CancelScope(shield=True)) that v01.04 originally fixed --
  including a test proving asyncio.shield() specifically does not work for
  this, reproducing why that fix needed anyio's own primitive
- Reconciliation: confirmed dead-socket broadcast survival, malformed-JSON
  handling, bad-token rejection, and graceful-disconnect seat release were
  already correctly implemented and tested from v01.03/v01.04 -- this
  version's real work was narrower than the roadmap's full DoD line list
- v05.01 code review: 1 fix-now (LOW) -- hardened a plausible, undemonstrated
  race in the new reconnect test's own assertion timing

Version 04.02.00 (2026-08-16)
-----------------------------
- scripts/run_arena.sh (new): one-command agent-vs-agent demo -- creates a
  match, launches agent 1, waits for its own join confirmation before
  launching agent 2 (the only real lever for distinct seats -- there is no
  --symbol flag), prints the Observe link, tails both agents' banter live,
  cleans up on Ctrl-C
- Verified live end-to-end three times against a real server, two real
  agents, and a real Anthropic API key -- a full game played to completion
  with Aggressor/Sentinel's contrast landing exactly as v04.01 designed;
  found and fixed three real bugs along the way: the script now prefers
  .venv/bin/python3, backgrounds tail -f so the shutdown trap fires
  promptly on a non-interactive kill (it previously hung forever), and
  guards against double-running the cleanup trap
- v04.02 code review: 1 fix-now -- a trailing slash in $SERVER_URL produced
  a false "server not reachable" error; now normalized on read
- Phase v04 (Agent-vs-Agent Orchestration) complete -- the v04 release gate

Version 04.01.00 (2026-08-16)
-----------------------------
- profiles/aggressive.yml: refined Aggressor's system_prompt to needle a
  cautious opponent by name -- tuned for contrast, not standalone flavor
- profiles/cautious.yml (new): Sentinel -- temperature 0.4 (vs Aggressor's
  0.9), a wary, blocking play style with dry, understated banter, a real
  tonal + stylistic contrast when watched head-to-head
- v04.01 code review: clean pass, no findings

Version 03.03.00 (2026-08-16)
-----------------------------
- web/app.js: chat panel -- post (guarded, non-optimistic), render, self/other
  styling derived from myPlayerName (the real, tested sender contract),
  auto-scroll, system notices on match start/end (ARENA-103)
- Corrected web_ui_specification.md §4.4: chat_message.sender is the
  participant's player_name, not a seat symbol as previously documented
- web/app.js: Observer role hardening -- chat input/send now gated on role
  (mySymbol !== null) as well as connection state, not connection state
  alone; explicit release-gate test coverage for the spectator submit_move
  refusal, symbol:null on Observe, and Host/Join defaulting to non-spectator
  (ARENA-104)
- v03.03 code review (v03 release gate): 1 fix-now -- every user's name
  prompt defaulted to the same literal "Player", so two participants who
  both accepted the default (the lowest-effort path) collided and each saw
  the other's chat rendered as their own; now a random per-prompt suffix

Version 03.02.00 (2026-08-16)
-----------------------------
- web/app.js: renderBoard -- nine cells created once, marks + playability
  driven by isGameActive && mySymbol !== null && currentTurn === mySymbol
  && validMoves.includes(i); Observers never playable (ARENA-100)
- web/app.js: handleCellClick -> submit_move (guarded, no optimistic update);
  game_over freezes every cell, sets the status label, and highlights the
  winning line for an X/O result (computed client-side, presentational
  only -- the server payload carries no cell info); error events surface
  visibly without closing the connection (ARENA-101)
- web/app.js: player cards (initPlayerCards is null-safe for Observers,
  falling back to index.html's own defaults) + active-turn highlight;
  resetBoardAndCards() runs before a same-tab re-host's new WebSocket even
  opens, so no stale board/highlight/error state survives into the next
  match (ARENA-102)
- v03.02 code review: clean pass, no findings -- traced the win-line/
  state_update ordering guarantee and the double-submit race against the
  real server code, cross-checked every DOM id app.js looks up against
  index.html

Version 03.01.00 (2026-08-16)
-----------------------------
- server/main.py: mounted /ui with a scoped Cache-Control: no-store middleware
  (ARENA-097)
- web/index.html + styles.css: the static arena shell -- app bar, stage, chat
  panel, dark glass aesthetic, responsive down to a single column, adapted
  from spec/ui_prototype.html (ARENA-098)
- web/app.js: Host/Join/Observe wired to the REST lobby + one WebSocket each,
  a live connection-status indicator, the full (never-truncated) match id
  display, and a routeEvent dispatcher covering all five WS events -- board
  and chat rendering land in v03.02/v03.03 (ARENA-099)
- v03.01 code review: 1 fix-now -- connect() never closed a prior WebSocket
  before opening a new one, so re-hosting/joining in the same tab leaked the
  old connection and its server-side seat forever; now closes the old socket
  first and guards all handlers against a stale connection

Version 02.03.00 (2026-08-16)
-----------------------------
- agent/agent.py: CLI args, join_match, AgentSession + real WS connect (ARENA-093)
- agent/agent.py: decide() -- prompt -> LLMClient -> validate -> retry -> random
  legal fallback, so the agent never stalls the match on a hallucinated move
  (ARENA-094)
- agent/agent.py: chat sent before submit_move on every on-turn decision, both
  sides' moves/chat recorded into memory, game_over handling, line-buffered
  stdout/stderr for the CLI (ARENA-095)
- Full end-to-end integration suite against a real server (ScriptedLLMClient
  throughout, zero paid API calls): a full game to game_over, and the
  retry-then-fallback path exercised live, not only in isolation (ARENA-096) --
  this is the v02 release gate
- v02.03 code review: 1 fix-now -- a socket that died between accept() and its
  own `joined` send left its already-assigned seat permanently leaked (the
  seat-cleanup path only knew about sockets that reached
  ConnectionManager.connect()); now released directly when that never happened
- also fixed during ARENA-095 validation: server/main.py sent `joined` to a
  newly-connected socket only after registering it for broadcasts, so a
  concurrent broadcast (e.g. the opponent's opening move) could beat it on the
  wire, violating "joined is always the first message a client receives"
  (architecture.md §6.2); reordered to send `joined` first

Version 02.02.00 (2026-08-16)
-----------------------------
- agent/profile.py: AgentProfile.load_from_yaml + profiles/aggressive.yml (ARENA-090)
- agent/memory.py: MemoryWindow -- a shared-eviction-bound rolling buffer of moves +
  chat (ARENA-091)
- agent/prompt.py: build_prompt -- persona + memory + pushed board/valid-moves composed
  into one prompt, no model call (ARENA-092)
- v02.02 code review: 1 fix-now -- opponent chat was embedded in the prompt with no
  framing distinguishing it from real instructions (a prompt-injection surface,
  blast radius capped by server-side move re-validation); now explicitly framed as
  non-instructional
- also fixed: bare uvicorn has no real WebSocket implementation -- a live server
  couldn't accept WS connections at all (invisible to TestClient-based tests since
  v01.04); now uvicorn[standard], verified against a real server + real websockets
  client end to end

Version 02.01.00 (2026-08-16)
-----------------------------
- agent/schemas.py: AgentResponse{move, comment} (ARENA-087)
- agent/llm.py: the LLMClient abstract seam + create_llm_client dispatch (ARENA-088)
- agent/llm.py: AnthropicHaikuClient -- structured output via tool-use, fail-fast on a
  missing API key, zero paid calls in the default suite (ARENA-089)
- v02.01 code review: 1 fix-now -- HAIKU_MODEL_ID was not a real Anthropic model id
  (corrected to claude-haiku-4-5-20251001); invisible to the mocked test suite by
  design, would only have surfaced on a live run

Version 01.04.00 (2026-08-16)
-----------------------------
- server/websockets.py: ConnectionManager (connect/disconnect+seat-release/broadcast
  with dead-socket pruning/send_to/close_room) + the pinned event/action envelopes
  (ARENA-083)
- WS endpoint GET /ws/match/{id}?token=: 4001 on bad token, joined on connect, malformed
  JSON -> error without dropping the connection, cleanup in finally -- hardened against
  the async-cancellation disconnect path with anyio.CancelScope(shield=True) (ARENA-084)
- chat action: persist + broadcast chat_message (ARENA-085)
- submit_move: the full move authority flow -- no-seat/out-of-turn/illegal-move all
  rejected via error with state unchanged; game_over + close_room on completion
  (ARENA-086)
- v01.04 code review: 1 deferred (broadcast write-serialization under concurrent
  senders -> v05.01); two real bugs (async-cancellation seat leak, close-room loop
  crash) were found and fixed during implementation, each with a regression test
- v01 release gate met: two raw WS clients play a full game (win and draw), exchange
  chat, and reach game_over with the room closed

Version 01.03.00 (2026-08-16)
-----------------------------
- server/auth.py: opaque token issue/validate, IssuedToken (ARENA-079)
- server/schemas.py: JoinRequest/CreateMatchResponse/JoinResponse/HealthResponse (ARENA-080)
- server/match.py: assign_symbol seat rule (spectator-safe, idempotent, X-then-O,
  match-full) + release_seat (ARENA-081)
- server/main.py: FastAPI app, GET /health, POST /lobby/match, POST /lobby/join
  (404 on unknown match, spectator flagged and never auto-seated) (ARENA-082)
- v01.03 code review: 1 fix-now -- assign_symbol could raise on a genuine concurrent
  seat-assignment race instead of resolving to X/O; now retries against a fresh read

Version 01.02.00 (2026-08-16)
-----------------------------
- server/database.py: async SQLAlchemy engine over sqlite+aiosqlite:///./arena.db,
  connect-time FK pragma, init_models() (ARENA-075)
- server/models.py: Match, Participant (UNIQUE(match_id, symbol)), Move, ChatMessage
  ORM tables (ARENA-076)
- server/repository.py: Repository CRUD -- create_match, get_match, add_participant,
  release_seat, log_move, log_chat (ARENA-077)
- reconstruct_game + current_turn: live state replayed from the move log, never stored
  as mutable fields; full persistence/integration test suite (ARENA-078)
- v01.02 code review: 1 fix-now -- current_turn could diverge from reconstruct_game's
  own replayed state on an invalid logged move; fixed to derive from the same board

Version 01.01.00 (2026-08-16)
-----------------------------
- Repo skeleton: pyproject.toml (explicit setuptools packages list, mypy --strict, pytest
  scoped to tests/), games/ package layout (ARENA-071)
- GameInterface: the abstract game-plug-in seam, with its contract test (ARENA-072)
- TicTacToe engine: 9-cell board, 8 winning lines, draw detection, never-raising
  apply_move (ARENA-073)
- Exhaustive TicTacToe unit suite: 27 tests covering every winning line, draw, and
  illegal-move category (ARENA-074)
- v01.01 code review: no findings
